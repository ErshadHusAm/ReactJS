import React from 'react'

function ColorComp({name}) {
    // if(name === 'black') throw new Error('chi chi chi')

    return (
        <div>
            <h1>{name}</h1>
        </div>
    )
}
export default ColorComp;